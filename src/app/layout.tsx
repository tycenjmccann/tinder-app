export const metadata = {
  title: "Progress Tracker",
  description: "Daily progress metrics API",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
